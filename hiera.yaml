---
:backends:
  - yaml
:hierarchy:
  - "%{clientcert}"
  - "%{application_tier}"
  - common
  - defaults
  - openstack-aio-01
:yaml:
  :datadir: '/etc/puppetlabs/puppet/environments/%{environment}/hieradata'
